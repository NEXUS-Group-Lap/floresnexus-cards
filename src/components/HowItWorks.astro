---
import { siteConfig } from '../config.ts';
import { Icon } from 'astro-icon/components';
import { howItWorksSteps } from '../content/how-it-works';
---

<section
  id="how-it-works"
  class="relative border-t border-[#D8D5CD] bg-[#F7F5F0] px-4 py-20 sm:px-6 lg:px-8"
>
  <div
    class="absolute bottom-8 left-8 h-16 w-16 border-b-2 border-l-2 border-[#D8D5CD]"
  >
  </div>
  <div
    class="absolute right-8 bottom-8 h-16 w-16 border-r-2 border-b-2 border-[#D8D5CD]"
  >
  </div>

  <div class="mx-auto max-w-7xl">
    <div class="scroll-fade-down mx-auto mb-16 max-w-3xl text-center">
      <h2 class="text-mono-black mb-4 text-4xl font-bold sm:text-5xl">
        {siteConfig.howItWorks.title}
      </h2>
      <p class="text-mono-dark text-xl">
        {siteConfig.howItWorks.subtitle}
      </p>
    </div>

    <div class="grid grid-cols-1 gap-8 md:grid-cols-2 lg:grid-cols-5">
      {
        howItWorksSteps.map((step, index) => (
          <div class="relative">
            <div
              class={`scroll-fade-${index % 2 === 0 ? 'left' : 'right'} rounded-2xl border border-[#D8D5CD] bg-white p-8 card-shadow stagger-${index + 1}`}
            >
              <div class="mb-6 flex h-12 w-12 items-center justify-center rounded-full bg-[#0B0B0B] text-xl font-bold text-white">
                {step.number}
              </div>
              <h3 class="text-mono-black mb-3 text-xl font-semibold">
                {step.title}
              </h3>
              <p class="text-mono-dark">{step.description}</p>
            </div>
            {index < howItWorksSteps.length - 1 && (
              <div class="absolute top-1/2 -right-4 hidden -translate-y-1/2 transform lg:block">
                <Icon
                  name="heroicons:chevron-right"
                  class="h-8 w-8 text-[#D8D5CD]"
                />
              </div>
            )}
          </div>
        ))
      }
    </div>
  </div>
</section>
